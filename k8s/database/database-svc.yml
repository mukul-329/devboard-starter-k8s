kind: Service
apiVersion: v1
metadata: 
  name: database          #You can replace it with your database name
  namespace: devboard
spec:
  type: ClusterIP
  selector: 
    devboard: database    #You can replace it with your database name
  ports:
    - protocol: TCP
      port: <port-number>           #Internal port services needed for ClusterIP
      targetPort: <port-number>     #Database Container port
